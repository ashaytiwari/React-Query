import React from "react";

const NotFound = () => {
  return (
    <h1
      style={{
        backgroundColor: "teal",
        color: "#fff",
        padding: "20px",
        textAlign: "center "
      }}
    >
      No Page Found
    </h1>
  );
};

export default NotFound;
